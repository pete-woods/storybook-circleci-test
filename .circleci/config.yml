version: 2.1

jobs:
  run:
    docker:
      - image: cimg/node:current
    steps:
      - checkout
      - run: npm install
      - run: npm run build-storybook
      - store_artifacts:
          path: storybook-static

workflows:
  run:
    jobs:
      - run